class Constant_values():

    def __init__(self, api_key, api_secret, *args, **kwargs):

        # General parameters, it is used for every exchange parameter class

        # Define the API Keys
        self.API_KEY = api_key
        self.API_SECRET = api_secret